export enum ErrorValidateMessage {
  required = 'This field is required',
  minLength = 'Minimum 8 characters required',
  validate = 'Passwords must match',
  pattern = 'Wrong or Invalid email address',
}
